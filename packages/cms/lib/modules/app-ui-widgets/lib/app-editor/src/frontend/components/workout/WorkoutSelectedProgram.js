import React  from "react";
import { View, Text, StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titleText: {
        fontSize: 20,
        fontWeight: "bold"
    }
});

const WorkoutSelectedProgram = (props) => {
    return <View>
        <Text> Your Program </Text>
    </View>
}

export default WorkoutSelectedProgram;
